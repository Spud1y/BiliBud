package data.structs;

/**
 * ENUM that the UI will use to allow the user to choose which units they are inputing
 */
public class BiliUnits {
    public enum Units {
        MML,
        MGDL
    }
}
